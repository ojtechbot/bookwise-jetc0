
'use client';

// This page re-uses the component from the student settings page.
// The component is already designed to handle both students (PIN) and staff (password).
export { default } from '@/app/dashboard/settings/page';
